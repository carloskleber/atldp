[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Windows port (ADR-0011): add the target locally / in CI when building it, e.g.
#   rustup target add x86_64-pc-windows-gnu
targets = ["x86_64-unknown-linux-gnu"]
